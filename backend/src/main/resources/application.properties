# Database configuration (Compatible with Railway default MySQL variables)
spring.datasource.url=jdbc:mysql://${MYSQLHOST:localhost}:${MYSQLPORT:3306}/${MYSQLDATABASE:premium_adventure}?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=${MYSQLUSER:root}
spring.datasource.password=${MYSQLPASSWORD:root}

# Hibernate configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT configuration
jwt.secret=${JWT_SECRET:9a67475d459d0287311185590c8839088825851416807cc902d1d4d732860a22}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Server configuration
server.port=${PORT:8080}

# Razorpay Configuration
razorpay.key.id=${RAZORPAY_KEY_ID:rzp_test_SgqwRbbG5qnBvy}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:tdmDdyR7jVKUWgpraxDVx2AG}
